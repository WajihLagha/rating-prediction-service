services:
  nlp-rating-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nlp-rating-service
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
